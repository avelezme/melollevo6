<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>

  <body>

    <nav>

    <table>
      <tr> <a href="/user_sign_in"><button>Sign In</button></a>
      </tr>

      <tr> <a href="/user_sign_up"><button>Sign Up</button></a>
      </tr>

      <tr> <a href="/user_sign_out"><button>Sign Out</button></a>
      </tr>
    </table>

      <ul>
        <li>
          <a href="/listings"> Listings (Index)</a>
        </li>
      </ul>

      <%= session.fetch(:user_id) %>
      

    </nav>

    <% if notice.present? %>
      <div style="border: thin green solid; color: green">
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div style="border: thin red solid; color: red">
        <%= alert %>
      </div>
    <% end %>


    <%= yield %>
  </body>
</html>
